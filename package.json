{
  "name": "ai-body-language-backend",
  "version": "1.0.0",
  "description": "Backend API for AI Body Language Analysis System",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && prisma generate && prisma migrate deploy",
    "start": "node dist/index.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "prisma": "^5.22.0",
    "typescript": "^5.7.2"
  }
}
